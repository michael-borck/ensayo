---
title: "Sample Policy"
type: policy
brief: "A fixture document used for standalone theme development."
---

# Sample Policy

_A fixture document used for standalone theme development._

This content is replaced by the Ensayo generator at build time. It exists so the
`tech-modern` theme renders something when run on its own with `npm run dev`.
