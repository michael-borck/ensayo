"""Ensayo Services — the FastAPI app, SQLite layer, and UC dashboard (Phase 3).

A single modular monolith (see docs/adr/0002). This subpackage holds the dynamic
side of the platform; the generator (``ensayo.generator``) is imported as a library.
"""
